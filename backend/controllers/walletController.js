const bcrypt = require("bcrypt");
const jwt = require("jsonwebtoken");

const SECRET_KEY = "super-secret-key";

exports.walletOperation = async (req, res) => {
    try {
        
    } catch (error) {
        res.status(500).json({ error: "Error " });
    }
};
